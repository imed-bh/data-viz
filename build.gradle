allprojects {
    repositories {
        mavenCentral()
    }
}

subprojects {
    group 'dataviz'
    version '1.0'

    apply plugin: 'java'

    sourceCompatibility = 1.10
    targetCompatibility = 1.10

    dependencies {
        testCompile 'junit:junit:4.12'
    }
}